<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sparta.java02.domain.user.repository.UserMapperRepository">
  <insert id="insertUser" parameterType="com.sparta.java02.domain.user.dto.UserDTO">
    INSERT INTO user VALUES
    <foreach collection="users" item="user" separator=",">
      (#{user.name}, #{user.email}, #{user.password}, now(), now())
    </foreach>
  </insert>

  <update id="updateUser" parameterType="com.sparta.java02.domain.user.dto.UserDTO">
    UPDATE user
       SET name = #{email}
    WHERE id = #{id}
  </update>

  <delete id="deleteUser" parameterType="Long">
    DELETE
    FROM user
    WHERE id = #{id}
  </delete>
</mapper>